# Generated by `devg init-env` from your host environment.
# Passed to the proxy sidecar. The app container never sees these.

# GitHub token (for gh proxy and MCP servers that use token_env)
# GH_TOKEN=$(gh auth token)
