# Env vars for the proxy sidecar. Evaluated by `kap sidecar-init`.
# Shell substitutions like $(cmd) are re-evaluated on each devcontainer start.

# GitHub token (for gh CLI proxy and MCP servers using token_env)
GH_TOKEN=$(gh auth token)
